# Server
PORT=3000
NODE_ENV=production
APP_ENV=production

# Auth
JWT_SECRET=replace-with-a-secure-secret

# MongoDB Atlas
MONGODB_URI=
MONGODB_DB_NAME=maiawall_homolog

# Frontend/CORS
FRONTEND_URL=http://localhost:4200
CORS_ORIGINS=http://localhost:4200

# Required only when intentionally running npm run seed outside local/dev/test.
# Example: CONFIRM_SEED=SEED maiawall_homolog
CONFIRM_SEED=
